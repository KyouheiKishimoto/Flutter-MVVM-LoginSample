import 'package:flutter/cupertino.dart';

class ListSubPage extends StatelessWidget {

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("subで作成したやつ"),
    );
  }
}